apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "panoptium.fullname" . }}-leader-election
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "panoptium.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "panoptium.fullname" . }}-leader-election
subjects:
  - kind: ServiceAccount
    name: {{ include "panoptium.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
