{{- if .Values.webhook.enabled }}
apiVersion: admissionregistration.k8s.io/v1
kind: MutatingWebhookConfiguration
metadata:
  name: {{ include "panoptium.fullname" . }}-mutating
  labels:
    {{- include "panoptium.labels" . | nindent 4 }}
  {{- if .Values.webhook.certManager }}
  annotations:
    cert-manager.io/inject-ca-from: {{ .Release.Namespace }}/{{ include "panoptium.fullname" . }}-webhook-cert
  {{- end }}
webhooks:
  - name: mpod.panoptium.io
    admissionReviewVersions: [v1]
    clientConfig:
      service:
        name: {{ include "panoptium.fullname" . }}-webhook
        namespace: {{ .Release.Namespace }}
        path: /mutate-v1-pod
      {{- if not .Values.webhook.certManager }}
      caBundle: Cg==
      {{- end }}
    failurePolicy: Ignore
    namespaceSelector:
      matchExpressions:
        - key: kubernetes.io/metadata.name
          operator: NotIn
          values:
            {{- range .Values.excludedNamespaces }}
            - {{ . | quote }}
            {{- end }}
    rules:
      - apiGroups: [""]
        apiVersions: [v1]
        operations: [CREATE]
        resources: [pods]
    sideEffects: None
{{- end }}
